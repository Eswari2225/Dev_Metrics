# Netlify Configuration for React/Vite App
# (Root-level netlify.toml takes precedence - this is backup)

[build]
  command = "npm run build"
  publish = "dist"

# Redirect all non-file requests to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
